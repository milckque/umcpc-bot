services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./meetings.json:/app/meetings.json
      - ./strikes.json:/app/strikes.json
      - ./club_info.json:/app/club_info.json